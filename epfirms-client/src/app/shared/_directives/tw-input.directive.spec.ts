import { TwInputDirective } from './tw-input.directive';

describe('TwInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TwInputDirective();
    expect(directive).toBeTruthy();
  });
});
